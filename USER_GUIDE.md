# 失意者联盟 - 使用指南

## 🎮 完整功能说明

### 1. 上传截图功能
1. 点击页面上的"上传截图"按钮或拖拽图片到上传区域
2. 系统会自动调用豆包 Vision API 识别 eFootball 比赛数据
3. 识别完成后，会显示确认卡片，包含：
   - 比赛结果（胜/平/负）
   - 控球率
   - 球员评分

### 2. 生成新闻报道
确认比赛结果后，系统会自动：
1. 生成一篇足球新闻风格的赛后快讯
2. 包含标题、正文和3个重点
3. 展示在页面顶部的"赛后快讯"板块

### 3. 自动评论生成
系统会根据每个球员的表现：
1. 调用豆包 Chat API 生成符合当前舆论氛围的评论
2. 评论风格受"敌对度"影响：
   - 高敌对度：攻击性评论（红色）
   - 低敌对度：追捧评论（绿色）
3. 评论自动添加到论坛帖子区

### 4. 救赎值系统
- 每场比赛后，球员的救赎值会根据评分变化：
  - **Rating > 7.5**：+15 分
  - **Rating < 5.0**：-5 分
  - **连续3场上升**：额外 +10 分
  - **教练维护**：下场比赛系数 ×1.5

### 5. 舆论敌对度
- 动态变化的敌对度影响评论风格
- 胜利降低敌对度，失败提升敌对度
- 玩家的回复和点踩也会影响敌对度

### 6. 互动功能
- **点赞/点踩评论**：影响敌对度
- **回复评论**：降低敌对度，展示"教练维护"
- **点击球员**：查看球员详情

## 📸 截图识别说明

### 支持的格式
- JPG / PNG / WEBP
- 最大 10MB
- 建议尺寸：1920×1080 或更高

### 最佳实践
1. **清晰的赛后截图**：确保球员评分清晰可见
2. **包含必要信息**：
   - 球员姓名
   - 位置
   - 评分
   - 控球率

### API 配置
如果你已配置豆包 API Key（在 `.env.local`），系统会自动使用真实 AI 识别。否则使用模拟数据供测试。

## 🎯 完整流程示例

### 第一步：上传截图
```
1. 点击"上传截图"按钮
2. 选择 eFootball 赛后截图
3. 等待识别（约 2-5 秒）
```

### 第二步：确认数据
```
识别结果会显示：
- 比赛结果：✓ 胜利
- 控球率：58%
- 球员表现：
  格列兹曼 CF 8.5
  库蒂尼奥 CAM 7.8
```

### 第三步：生成内容
点击"确认并生成评论"后：
```
1. 生成新闻报道（顶部展示）
2. 更新球员救赎值
3. 调整舆论敌对度
4. 生成 AI 评论
5. 自动滚动到顶部查看新闻
```

### 第四步：互动体验
```
- 阅读新闻报道
- 查看球员救赎值变化
- 阅读网友评论
- 回复/点赞/点踩评论
```

## ⚙️ 豆包 API 配置

### 1. 获取 API Key
访问豆包开放平台获取你的 API Key

### 2. 配置环境变量
编辑 `.env.local` 文件：
```env
VITE_DOUBAO_API_KEY=your_actual_api_key_here
VITE_DOUBAO_MODEL=doubao-seed-1-8-251228
```

### 3. 重启开发服务器
```bash
npm run dev
```

## 🐛 常见问题

### Q: 上传图片后没有反应？
A: 检查浏览器控制台是否有错误。如果 API Key 未配置，系统会使用模拟数据。

### Q: 识别结果不准确？
A: 豆包 Vision API 可能需要多次尝试。确保截图清晰，信息完整。

### Q: 评论都是中文的吗？
A: 是的，系统专门设计为中文足球论坛风格，支持现代足球梗。

### Q: 如何重置所有数据？
A: 在浏览器控制台执行 `localStorage.clear()`，然后刷新页面。

### Q: 可以添加自己的球员吗？
A: 目前需要修改 `src/utils/storage.js` 中的 `initializeDefaultData()` 函数。

## 🎨 自定义

### 修改球员列表
编辑 `src/utils/storage.js`：
```javascript
const defaultPlayers = [
  {
    id: '1',
    name: '你的球员名',
    nickname: '球员外号',
    backstory: '背景故事',
    redemptionScore: 10,
    history: []
  }
];
```

### 调整救赎算法
编辑 `src/models/DataModels.js` 中的 `calculateRedemptionChange()` 函数。

### 修改舆论模型
编辑 `src/engine/GameEngine.js` 中的 `SentimentEngine` 类。

## 🚀 部署

### 构建生产版本
```bash
npm run build
```

### 部署到 Vercel
```bash
vercel --prod
```

### 部署到 Netlify
```bash
netlify deploy --prod --dir=dist
```

## 📝 技术栈

- **前端框架**：React 18
- **构建工具**：Vite 5
- **AI 服务**：豆包 Vision & Chat API
- **存储**：LocalStorage
- **样式**：纯 CSS（复古 BBS 风格）

## 🎉 享受游戏！

现在你可以：
1. 上传真实的 eFootball 比赛截图
2. 查看 AI 生成的新闻报道
3. 阅读键盘侠评论
4. 见证球员从"失意者"到"救赎者"的蜕变

祝你体验愉快！⚽
